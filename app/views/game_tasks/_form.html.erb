<%= form_with(model: [@route, @game_task], local: true) do |form| %>
  <% if @game_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_task.errors.count, "error") %> prohibited this game_task from being saved:</h2>

      <ul>
        <% @game_task.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div id="map" data-lat="<%= @route.latitude %>" data-long="<%= @route.longitude %>"> </div>
  
  <div class="input">
    <%= form.label 'Ortshinweis', :class => 'input-label' %>
    <%= form.text_field :hint, :class => 'input-text' %>
  </div>

  <div class="input">
    <%= form.label :latitude, :class => 'input-label' %>
    <%= form.text_field :latitude, :class => 'input-text' %>
  </div>

  <div class="input">
    <%= form.label :longitude, :class => 'input-label' %>
    <%= form.text_field :longitude, :class => 'input-text' %>
  </div>

  <div> Aufgabe hinzufügen <%= MultipleChoice.to_s %> </div> 
  <div> Aufgabe hinzufügen <%= TrueFalse.to_s %> </div> 
  <div> Aufgabe hinzufügen <%= TextInput.to_s %> </div> 
  <div> Aufgabe hinzufügen <%= PhotoUpload.to_s %> </div> 

  <div class="input">
    <%= form.label :name, :class => 'input-label' %>
    <%= form.text_field :name, :class => 'input-text' %>
  </div>

  <div class="input">
    <%= form.label 'Frage', :class => 'input-label' %>
    <%= form.text_field :description, :class => 'input-text' %>
  </div>

  <div class="input">
    <%= form.label "Antwort(en)", :class => 'input-label' %>
    <%= form.text_field :answer, :class => 'input-text' %>
  </div>

  <div class="actions">
    <%= form.submit 'Erstellen', :class => 'btn' %>
  </div>
<% end %>
