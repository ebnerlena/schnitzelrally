  <h2> The next destination is... </h2>
  <p class="hint"> Hint: <%= game_task.hint %> </p>
  
  <div id="map" data-lat="<%= route.latitude %>" data-long="<%= route.longitude %>"> </div>

<%= button_to 'I am there', player_path(current_or_guest_user.player), { action: "update", method: "patch", params: {"player" => { state: "answering" }}, class: 'btn' } %>
