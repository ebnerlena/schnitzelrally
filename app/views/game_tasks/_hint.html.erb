  <h2> The next destination is... </h2>
  <p class="hint"> Hint: <%= @game_task.hint %> </p>
  
  <div id="map" data-lat="<%= @route.latitude %>" data-long="<%= @route.longitude %>"> </div>

  <%= link_to "I'm here", route_game_task_path(@route, @game_task), {class: 'btn' } %>

<button id="arrived_btn" data-player_id="<%= current_or_guest_user.player.id %>">We are here</button>